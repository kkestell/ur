# ur

## What is ur?
One-paragraph orientation: an async Rust library for tool-using LLM agents over a pluggable `Provider` trait.

## Install
The `Cargo.toml` snippet (`ur-rs`, tokio, futures-util) and the minimum API-key requirement.

## Features
The bulleted capability list (streaming deltas, tool dispatch with rollback, macros, structured outputs, pluggable providers).

## Where to start
A "pick a path" guide routing readers to quick-start, then concepts, then reference docs.

## Reference docs
The ordered table of contents linking each doc with a one-line description.
