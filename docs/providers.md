# Providers

## What is a provider?
The `Provider` trait as the pluggable seam that drives one normalized model turn.

## Shipped providers
A comparison table: OpenAI, DeepSeek, OpenRouter — feature, default?, and distinguishing notes.

## Feature flags and installation
How the facade enables providers (`openai` default; `deepseek`/`openrouter` opt-in) and the `Cargo.toml` forms.

## Choosing a provider
Guidance by need: default OpenAI, reasoning/price (DeepSeek), model breadth (OpenRouter).

## Per-provider references
Links to the openai, deepseek, openrouter, and custom-provider docs.
