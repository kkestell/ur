# Quick Start

## Prerequisites
Rust toolchain/MSRV, the `Cargo.toml` dependencies, and setting `OPENAI_API_KEY`.

## The smallest agent
The minimal end-to-end example: client → model → agent → session → stream loop.

## Walkthrough
A brief annotation of each line of the minimal example and what role it plays.

## Adding a tool
Extending the example with one `#[ur::tool]` fn registered via `.tool(...)`.

## Running it
`cargo run` invocation and what output to expect.

## Next steps
Pointers to agents, tools, streaming, and provider docs to go deeper.
